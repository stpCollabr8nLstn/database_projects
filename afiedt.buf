SELECT beer, rating AS score
FROM myveryownallratings 
WHERE rating = (
	SELECT MAX(rating) 
	FROM myveryownallratings
	WHERE rating < (
		SELECT MAX(rating)
		FROM myveryownallratings
	)
)
UNION
SELECT * from myveryowntopratedbeer 
/
